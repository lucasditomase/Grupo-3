import { StyleSheet } from 'react-native';

/**
 * Light theme styles for the application.
 */
const themeLight = StyleSheet.create({
    // Default container style for light mode
    defaultContainer: {
        backgroundColor: 'white',
        flex: 1,
    },

    // Background style for screens with light mode
    lightBackground: {
        backgroundColor: 'white',
    },
});

export default themeLight;
